import * as React from 'react';
import { Container } from '@src/app/components/containers';

export default function Home(): React.ReactElement {
  return (
    <Container>
      <span>The Show Must Go On</span>
    </Container>
  );
}
